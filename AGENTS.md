# WeReadAura Engineering Rules

本文件定义 `WeReadAura` 仓库在当前阶段的统一工程约束。在没有更高优先级指令时，所有实现、重构、测试、文档更新与 AI 协作行为都应以本文件为准。

## 1. 项目概况

- 项目名称：`WeReadAura`
- 项目目标：构建面向个人用户的微信读书阅读分析工具
- 当前阶段：`MVP 已编码并进入持续迭代阶段`
- 核心方向：`只读分析、聚合展示、趋势复盘、洞察解释`
- 基本原则：先确认边界，再推进实现；先保证数据正确，再优化表达与界面复杂度

## 2. 规则目标

本文件主要约束以下内容：

- 功能边界与产品范围
- 工程分层与目录约定
- 数据口径与统计一致性
- 前端视觉与交互实现方式
- 安全、隐私、测试和文档要求
- 当前阶段实现与后续规划的区分

## 3. 总体开发原则

- 所有功能都必须服务于“个人阅读分析”主目标，避免功能蔓延。
- 首版默认围绕只读分析能力设计，不实现写回、公开分享或社交化能力。
- 任何实现都必须能映射到需求文档中的明确功能点。
- 当上游接口能力与需求不一致时，以真实可获取数据为准，并同步更新文档。
- 所有改动应保持小步推进，确保可验证、可回滚、可解释。
- 如发现需求外扩或决策影响面超出当前范围，应先暂停实现并回到文档确认。

## 4. 当前实现状态

截至当前仓库状态，项目已经具备可运行的 MVP 骨架，后续规则必须建立在这一现实基础上，而不是继续按“尚未编码”的前提书写。

当前已落地：

- `Next.js App Router + TypeScript`
- `Route Handlers` 作为内部 BFF
- `src/server/services/*` 作为聚合与编排层
- `src/server/adapters/weread/*` 作为微信读书 Skill 适配层
- `src/server/cache/sync-cache.ts` 作为本地同步快照缓存
- 总览、书架、统计、笔记、发现、书籍详情、设置等页面

当前尚未落地：

- PostgreSQL 持久化
- Drizzle ORM
- 独立 `repositories / db` 层
- Zod DTO 校验层
- TanStack Query

因此，后续文档与实现必须明确区分：

- `当前实现`
- `目标架构`
- `未来规划`

禁止把未来规划写成已经实现的事实。

## 5. 技术决策原则

- 优先选择成熟、稳定、低维护成本的技术方案。
- 前后端边界必须明确，采集、清洗、聚合、展示分层处理。
- 业务逻辑不得散落在视图层或重复出现在多个位置。
- 所有统计指标必须有唯一口径定义，禁止多处重复计算且口径不一致。
- 第三方依赖应保持精简；新增依赖前先确认是否确有必要。
- 涉及 AI 能力时，优先采用“确定性信号层 + 有边界的模型归纳”方案，而不是把原始数据直接交给模型自由发挥。

## 6. 目录与文件约定

- 根目录保留项目级说明文件，如 `README.md`、`AGENTS.md`、环境模板等。
- 需求、方案、接口、测试设计、AI 设计文档统一放在 `docs/`。
- 源码目录按职责分层，不按临时页面堆叠。
- 公共类型、常量、配置、工具函数必须集中管理，避免重复定义。
- 测试文件与被测试模块应保持清晰的追踪关系。

当前目录约定以真实实现为准：

- 页面与 API：`src/app/*`
- 通用与业务组件：`src/components/*`
- 共享工具与类型：`src/lib/*`
- 服务端聚合与适配：`src/server/*`
- 样式与主题：`src/styles/*`

当前业务组件实际位于 `src/components/features/*`。在未先做结构调整前，新增能力应优先沿用这一现实目录布局。

## 7. 编码规范

- 默认使用 ASCII 编写代码；仅在现有文件已使用非 ASCII 且确有必要时例外。
- 变量、函数、类型、文件命名必须语义明确，避免缩写堆积。
- 函数应尽量保持单一职责；复杂逻辑应拆分为可测试的小函数。
- 禁止在同一函数中同时承担数据拉取、数据转换和界面拼装三类职责。
- 优先使用纯函数处理统计、聚合和格式转换逻辑。
- 注释仅用于解释复杂、关键或不直观逻辑，避免无信息量注释。
- 不保留废弃代码、调试日志、临时分支判断或大块注释掉的旧实现。

## 8. 数据与业务规则

- 所有来自微信读书的数据都必须经过类型校验、字段兜底和异常场景处理。
- 任何时间、进度、统计值都必须明确单位、来源和口径。
- 阅读分析相关核心口径必须集中定义，至少包括：
  - `阅读时长` 的计算口径
  - `完成一本书` 的判断标准
  - `活跃阅读日` 的判断规则
  - `划线数` 与 `笔记数` 的去重规则
- 任何聚合结果都应可通过原始数据复算。
- 不在前端保存敏感令牌，不把敏感数据写死在仓库中。
- 涉及 AI 生成的洞察、人格、偏好、行为等内容，必须能回溯到明确的结构化事实或证据字段。

## 9. API 与数据访问规范

- API 调用层统一封装，禁止在页面组件中散落请求逻辑。
- 请求、响应、错误处理、重试、超时策略应保持一致。
- 所有外部接口都应定义明确的输入输出类型。
- 对上游字段不稳定的场景，必须做兼容处理和降级展示。
- 接口失败时优先返回可理解的错误状态，禁止静默吞错。
- 当前实现默认优先读取同步快照；数据库持久化未落地前，不得在文档中表述为“页面默认读数据库”。

## 10. 前端实现规则

- 页面优先呈现核心信息，不堆砌装饰性图表。
- 同一指标在不同页面中的文案、单位、颜色语义必须保持一致。
- 视觉组件与业务逻辑分离，避免组件内部隐藏统计口径。
- 列表、图表、筛选器必须考虑空态、加载态、失败态。
- 首版优先保证桌面端体验，同时兼顾移动端基本可用。
- 图表必须有文字指标兜底，不能只依赖视觉图形表达。

### 10.1 视觉方向

- 全站统一采用 plain neo-brutalism 方向。
- 基础交互组件来自本地克隆的 [neobrutalism-components](https://github.com/ekmas/neobrutalism-components)（`neobrutalism-components-local/`，不入库），经 `src/components/ui/` 包装后对外使用。
- 字体栈以项目 `layout.tsx` / `globals.css` 为准，不因组件库引入而改变。
- 默认使用纸张感浅底色、黑色边框与无 blur 的硬阴影。
- 禁止使用 glassmorphism、柔和 SaaS 渐变、大面积 blur 和模板化创业公司首页风格。
- 标题必须大、直接、可读；CTA 必须明显；装饰优先级低于信息优先级。
- 如使用图表库或现成 UI 库，必须通过主题或二次封装统一到项目视觉语言，不能保留原生 SaaS 皮肤。
- 移动端允许适当减弱阴影位移，但不能丢掉边框和结构感。

### 10.2 设计系统规则

- 开发前先建立 design token 层，至少包含颜色、边框、阴影、圆角、容器宽度和字号体系。
- 不允许在页面中大量散落 one-off 颜色、阴影、圆角和边框写法。
- 优先沉淀以下基础组件：
  - `Button`
  - `Card`
  - `Input`
  - `Badge`
  - `Section`
  - `MetricCard`
  - `BookCard`
  - `RecommendationCard`
  - `ChartCard`
- 基础 `Button / Card / Badge / Input` 优先走 `src/components/ui/` 包装层，不为单页效果随意派生平行组件。
- 新增 neobrutalism 组件时从 `@neo/components/ui/*` 引用，不得把 `neobrutalism-components-local/` 提交进 git。

### 10.3 交互与可访问性规则

- hover 和 active 动效应体现轻微物理按压感，优先使用 `transform` 与硬阴影变化。
- 避免复杂连续动画、视差和装饰性滚动特效。
- 必须提供清晰的 `focus-visible` 状态。
- 不允许仅靠颜色表达状态，阅读状态、推荐反馈等必须有文本辅助。
- 必须支持 `prefers-reduced-motion`。

## 11. 后端、分析与 AI 规则

- 数据采集、数据清洗、指标计算、洞察生成、报告生成必须分层实现。
- 聚合逻辑应可单测，不依赖页面运行才能验证。
- 对耗时统计任务预留缓存或异步化空间。
- 所有报告生成逻辑都要避免把展示格式和计算逻辑耦合在一起。

涉及 AI 功能时，额外遵循：

- 模型不负责定义统计口径，只负责解释、归纳和表达。
- 页面型 AI 输出优先使用结构化 JSON，而不是自由文本。
- 上下文必须按任务裁剪，只传必要字段，禁止把无关全量数据直接交给模型。
- 对话式 AI、页面洞察、阶段性总结应优先复用同一套事实层和规则层。

## 12. 测试要求

- 所有核心业务逻辑都必须配套自动化测试。
- 至少应覆盖以下内容：
  - 书架数据解析
  - 阅读统计口径计算
  - 划线与笔记聚合
  - 单书进度汇总
  - 推荐数据适配与兜底
- 修复缺陷时应优先补回归测试。
- 对复杂转换逻辑优先做单元测试，对关键用户流程补集成测试。
- 对 AI 洞察相关能力，至少应补充：
  - schema 校验测试
  - 降级逻辑测试
  - mock / live / 数据稀疏场景测试

## 13. 文档要求

- 新增重要功能前先补需求或设计说明，避免直接开写。
- 当实现与原需求有差异时，代码与文档必须同步更新。
- 所有关键口径、限制、降级策略和边界条件都应留下文档记录。
- 对外部依赖、环境变量、启动方式和当前实现状态必须写清楚。
- 文档必须明确区分：
  - 当前实现
  - 目标架构
  - 后续规划

## 14. 安全与隐私

- 默认将用户阅读数据视为敏感个人数据。
- 不在日志中输出完整笔记内容、令牌或用户标识。
- 调试样例数据必须使用脱敏内容。
- 数据缓存、导出、分享相关能力必须显式受控。
- AI 相关功能不得把原始上游 payload、完整笔记正文或敏感身份字段直接发送给模型。

## 15. 提交与评审要求

- 每次改动应聚焦单一目标，避免把无关重构混入同一次提交。
- 提交前至少完成自检：功能、测试、类型、文档。
- 评审优先关注数据正确性、边界处理、口径一致性和可维护性。
- 如发现实现偏离需求或文档，必须先修正文档或实现，不允许长期“口头对齐”。

## 16. 当前阶段附加要求

- 当前仓库已完成可运行的 Next.js MVP 骨架，新增重要能力前仍应先补技术方案。
- 技术方案至少回答以下问题：
  - 准备使用什么技术栈
  - 微信读书 Skills 的接入方式是什么
  - 前后端是否分离
  - 数据缓存和鉴权如何处理
  - 首批页面和接口如何拆分
  - AI 功能如何与当前事实层、快照层和页面结构对接
- 当前技术方案主文档为 [docs/technical-architecture.md](D:\WeReadAura\docs\technical-architecture.md)。

## 17. 文档优先级与关系

产品与工程文档按以下关系理解：

- 产品需求以 [docs/weread-reading-analytics-prd.md](D:\WeReadAura\docs\weread-reading-analytics-prd.md) 为主。
- 前端视觉规范以 [docs/frontend-visual-style-guide.md](D:\WeReadAura\docs\frontend-visual-style-guide.md) 为主。
- 字段排版选型以 [docs/field-typography-guide.md](D:\WeReadAura\docs\field-typography-guide.md) 为准。
- 工程实现与分层以 [docs/technical-architecture.md](D:\WeReadAura\docs\technical-architecture.md) 为主。
- AI 能力与界面规划以以下文档为主：
  - [docs/ai-assistant-architecture.md](D:\WeReadAura\docs\ai-assistant-architecture.md)
  - [docs/ai-insight-schema-design.md](D:\WeReadAura\docs\ai-insight-schema-design.md)
  - [docs/ai-ui-surface-plan.md](D:\WeReadAura\docs\ai-ui-surface-plan.md)

本文件负责约束“怎么实现”，不替代“做什么”。若规则、需求与实现出现冲突，应先回到文档确认，再更新规则或需求，不允许长期依赖口头约定。
