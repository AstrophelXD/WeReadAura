# WeReadAura Coding Rules

本文件定义本项目后续开发阶段统一遵循的工程规则。在没有更高优先级指令时，所有实现、重构、测试和文档更新都应以本文件为准。

## 1. 项目定位

- 项目名称：`WeReadAura`
- 项目目标：构建微信读书个人阅读分析工具
- 当前阶段：需求与规范阶段，暂未进入正式编码
- 开发原则：先定义边界，再做实现；先保证数据正确，再追求页面复杂度

## 2. 总体开发原则

- 所有功能必须服务于“个人阅读分析”主目标，避免功能蔓延
- 首版默认围绕只读分析能力设计，不实现对外分享和写回操作
- 任何实现都必须能映射到需求文档中的明确功能点
- 当接口能力与需求不一致时，以真实可获取数据为准，并同步更新文档
- 小步提交，保持每次改动可验证、可回滚、可解释

## 3. 技术决策原则

- 优先选择成熟、易维护、生态稳定的技术方案
- 优先保证数据结构清晰，避免把业务逻辑散落在视图层
- 前后端边界明确，采集、清洗、聚合、展示分层处理
- 所有统计指标必须有唯一口径定义，禁止在多个地方重复计算且口径不一致
- 第三方依赖尽量精简，新增依赖前先确认是否真的必要

## 4. 目录与文件约定

- 根目录保留项目级说明文件，如 `README.md`、`AGENTS.md`、环境模板等
- 需求、方案、接口、测试设计等文档统一放在 `docs/`
- 源码目录按职责分层，不按临时页面堆叠
- 公共类型、常量、配置、工具函数必须集中管理，避免重复定义
- 测试文件与被测试模块保持可追踪关系

## 5. 编码规范

- 默认使用 ASCII 字符编写代码，除非现有文件已使用非 ASCII 且确有必要
- 变量、函数、类型、文件命名应语义明确，避免缩写堆积
- 函数尽量保持单一职责，复杂逻辑拆成可测试的小函数
- 禁止在一个函数中同时承担数据拉取、数据转换和界面拼装三类职责
- 优先使用纯函数处理统计、聚合和格式转换逻辑
- 只在复杂或不直观的逻辑前添加简短注释，避免无信息量注释
- 不保留废弃代码、调试日志、临时分支判断和注释掉的大块旧实现

## 6. 数据与业务规则

- 所有来自微信读书的数据都要经过类型校验和字段兜底
- 任何时间、进度、统计值都必须明确单位和来源
- 阅读分析相关的核心口径需要集中定义，例如：
- `阅读时长` 的计算口径
- `完成一本书` 的判断标准
- `活跃阅读日` 的判断规则
- `划线数` 与 `笔记数` 的去重规则
- 任何聚合结果都应可通过原始数据复算
- 不在前端保存敏感令牌，不把敏感数据写死在仓库中

## 7. API 与数据访问规范

- API 调用层统一封装，禁止在页面组件中直接散落请求逻辑
- 请求、响应、错误处理、重试、超时策略保持一致
- 所有外部接口都需要定义明确的输入输出类型
- 对上游字段不稳定的场景，必须做兼容处理和降级展示
- 接口失败时优先返回可理解的错误状态，不静默吞错

## 8. 前端规则

- 页面优先呈现核心信息，不堆砌装饰性图表
- 同一指标在不同页面中的文案、单位、颜色语义保持一致
- 视觉组件与业务逻辑分离，避免组件内部隐藏统计口径
- 列表、图表、筛选器都需要考虑空态、加载态、失败态
- 首版优先保证桌面端体验，同时兼顾移动端基本可用
- 图表必须有文字指标兜底，不能只依赖视觉图形表达
- 统一采用 plain neo-brutalism 视觉方向；基础交互组件来自本地克隆的 [neobrutalism-components](https://github.com/ekmas/neobrutalism-components)（`neobrutalism-components-local/`，不入库），经 `src/components/ui/` 包装后对外使用，规范见 [docs/frontend-visual-style-guide.md](D:\WeReadAura\docs\frontend-visual-style-guide.md)
- 字体栈以项目 `layout.tsx` / `globals.css` 为准，不因引入组件库而更换
- 页面结构必须清晰可扫读，不通过微弱分隔线和细腻留白硬撑层级
- 默认使用纸张感浅底色、黑色边框和无 blur 的硬阴影
- 禁止使用 glassmorphism、柔和 SaaS 渐变、大面积 blur 和模板化创业公司首页风格
- 标题要大、直接、可读，CTA 要明显，装饰优先级低于信息优先级
- 如果使用图表库或现成 UI 库，必须通过主题或二次封装统一到项目视觉语言，不能保留原生 SaaS 皮肤
- 移动端允许适当减弱阴影位移，但不能丢掉边框和结构感

## 8.1 设计系统规则

- 开发前先建立 design token 层，至少包含颜色、边框、阴影、圆角、容器宽度和字号体系
- 不允许在页面里大量散落 one-off 颜色、阴影、圆角和边框写法
- 推荐优先抽象这些基础组件：
- `Button`
- `Card`
- `Input`
- `Badge`
- `Section`
- `MetricCard`
- `BookCard`
- `RecommendationCard`
- `ChartCard`
- 基础 `Button` / `Card` / `Badge` / `Input` 优先走 `src/components/ui/` 包装层，不为单页效果随意派生平行组件
- 新增 neobrutalism 组件时从 `@neo/components/ui/*` 引用，勿把 `neobrutalism-components-local/` 提交进 git

## 8.2 交互与可访问性规则

- hover 和 active 动效应体现轻微物理按压感，优先使用 `transform` 和硬阴影变化
- 避免复杂连续动画、视差和装饰性滚动特效
- 必须提供清晰的 `focus-visible` 状态
- 不允许仅靠颜色表达状态，阅读状态、推荐反馈等必须有文本辅助
- 必须支持 `prefers-reduced-motion`

## 9. 后端与分析规则

- 数据采集、数据清洗、指标计算、报告生成分层实现
- 聚合逻辑应可单测，不依赖页面运行才能验证
- 对耗时统计任务预留缓存或异步化空间
- 所有报表生成逻辑都要避免把展示格式和计算逻辑耦合在一起

## 10. 测试要求

- 所有核心业务逻辑必须配套自动化测试
- 至少覆盖以下内容：
- 书架数据解析
- 阅读统计口径计算
- 划线与笔记聚合
- 单书进度汇总
- 推荐数据适配与兜底
- 修复缺陷时应优先补回归测试
- 对复杂转换逻辑优先做单元测试，对关键用户流程补集成测试

## 11. 文档要求

- 新增功能前先补需求或设计说明，避免直接开写
- 当实现与原需求有差异时，代码和文档必须同步更新
- 所有关键口径、限制、降级策略都需要留下文档记录
- 对外部依赖、环境变量、启动方式必须写清楚

## 12. 安全与隐私

- 默认将用户阅读数据视为敏感个人数据
- 不在日志中输出完整笔记内容、令牌或用户标识
- 调试样例数据应使用脱敏内容
- 数据缓存、导出、分享相关能力必须显式受控

## 13. 提交与评审要求

- 每次改动聚焦单一目标，避免把无关重构混入同一次提交
- 提交前至少完成自检：功能、测试、类型、文档
- 评审优先关注数据正确性、边界处理、口径一致性和可维护性
- 发现需求外扩时先暂停实现，回到需求文档确认范围

## 14. 当前阶段附加要求

- 当前仓库尚未开始编码，任何人开始实现前都应先补技术方案
- 技术方案至少回答以下问题：
- 准备使用什么技术栈
- 微信读书 Skills 的接入方式是什么
- 前后端是否分离
- 数据缓存和鉴权如何处理
- MVP 首批页面和接口如何拆分
- 当前技术方案文档为 [docs/technical-architecture.md](D:\WeReadAura\docs\technical-architecture.md)

## 15. 与需求文档的关系

- 产品需求以 [docs/weread-reading-analytics-prd.md](D:\WeReadAura\docs\weread-reading-analytics-prd.md) 为主
- 前端视觉规范以 [docs/frontend-visual-style-guide.md](D:\WeReadAura\docs\frontend-visual-style-guide.md) 为主
- 技术方案以 [docs/technical-architecture.md](D:\WeReadAura\docs\technical-architecture.md) 为主
- 本文件负责约束“怎么实现”，不替代“做什么”
- 若两者冲突，先回到需求确认，再更新规则或需求，不允许口头约定替代文档
